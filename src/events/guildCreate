const { Events } = require("discord.js");

module.exports = {
  name: Events.GuildCreate,
  async execute(guild, DbConnection) {
    console.log(`Updating servers table`);
    if(!DbConnection.isInServers(guild.id)){
      DbConnection.insertIntoServers(guild.id)
    }
    // DbConnection.
  },
};